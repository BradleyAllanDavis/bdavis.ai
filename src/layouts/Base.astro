---
import '../styles/global.css';
import Icon from '../components/Icon.astro';

interface Props {
  title?: string;
  description?: string;
  current?: string;
}
const {
  title = 'Bradley Davis',
  description = 'Senior backend engineer, ex-OKX, ex-Intuit. Building AI-native systems and a Nix homelab.',
  current,
} = Astro.props;

// accent: which per-page nav hue [data-accent] picks up on hover/active (see
// global.css). Home/Resume intentionally omit it and fall through to the
// default --link blue -- Resume already reads as "backend" in blue, and Home
// is the site's identity color, not a themed section.
const nav = [
  { href: '/', label: 'Home', key: 'home' },
  { href: '/bio', label: 'Bio', key: 'bio' }, // identity page, same as Home/Resume: no accent
  { href: '/resume', label: 'Resume', key: 'resume' },
  { href: '/projects', label: 'Projects', key: 'projects', accent: 'projects' },
  { href: '/now', label: 'Now', key: 'now', accent: 'now' },
];
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <meta name="color-scheme" content="light dark" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    {/* Apply stored theme before paint to avoid a flash. */}
    <script is:inline>
      (function () {
        try {
          var t = localStorage.getItem('theme');
          if (t === 'light' || t === 'dark') {
            document.documentElement.setAttribute('data-theme', t);
          }
        } catch (e) {}
      })();
    </script>
  </head>
  <body>
    <div class="wrap">
      <header class="site-head">
        <a class="brand" href="/">Bradley Davis</a>
        <nav class="site-nav" aria-label="Primary">
          {nav.map((item) => (
            <a
              href={item.href}
              data-accent={item.accent}
              aria-current={current === item.key ? 'page' : undefined}
            >{item.label}</a>
          ))}
          <button class="theme-toggle" type="button" aria-label="Toggle color theme" data-theme-toggle>
            theme
          </button>
        </nav>
      </header>

      <main>
        <slot />
      </main>

      <footer class="site-foot">
        <span>&copy; {new Date().getFullYear()}</span>
        <span class="social-links">
          <a href="mailto:hire@bdavis.ai">hire@bdavis.ai</a>
          <a href="https://github.com/BradleyAllanDavis" aria-label="GitHub"><Icon name="github" size={16} /></a>
          <a href="https://www.linkedin.com/in/BradleyAllanDavis" aria-label="LinkedIn"><Icon name="linkedin" size={16} /></a>
          <a href="https://x.com/BradleyADavis" aria-label="X (Twitter)"><Icon name="x" size={16} /></a>
        </span>
      </footer>
    </div>

    <script is:inline>
      (function () {
        var btn = document.querySelector('[data-theme-toggle]');
        if (!btn) return;
        function current() {
          var explicit = document.documentElement.getAttribute('data-theme');
          if (explicit) return explicit;
          return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        }
        btn.addEventListener('click', function () {
          var next = current() === 'dark' ? 'light' : 'dark';
          document.documentElement.setAttribute('data-theme', next);
          try { localStorage.setItem('theme', next); } catch (e) {}
        });
      })();
    </script>
  </body>
</html>
